{
  "name": "twikoo-pkg",
  "version": "0.0.0",
  "private": true,
  "description": "Twikoo SEA 可执行产物打包流水线（不发布，version 为占位值）",
  "license": "MIT",
  "scripts": {
    "build:exe": "tsdown",
    "start": "node --experimental-strip-types src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "commander": "15.0.0",
    "tkserver": "workspace:*"
  },
  "devDependencies": {
    "@imaegoo/node-ip2region": "^2.1.1",
    "@tsdown/exe": "0.23.0",
    "@types/node": "^22.20.2",
    "tsdown": "0.23.0"
  }
}
